/* eslint-disable @typescript-eslint/no-var-requires */
const chai = require("chai");

describe("Tests tes", () => {
  it("verifies successful sayCpuReport", () => {
    chai.expect(true).to.be.equal(true);
  });
});
